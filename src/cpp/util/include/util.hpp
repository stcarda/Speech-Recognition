#include <array>
#include <iostream>
#include <map>
#include <sstream>
#include <string>
#include <vector>

using namespace std;

/**
 * This function accepts a string and a delimiter and splits the string 
 * at the delimeter character. Returns a vector of the words resulting from
 * the split.
*/
vector<string> SplitString(string str, char delimiter);

/**
 * This function converts a string to all upper-case characters.
*/
string ToUpper(string str);

/**
 * This function converts a string to all lower-case characters.
*/
string ToLower(string str);